$(function(){
  // // console.log($('#nav').width());
  // // var img = new Image();
  // //
  // // img.src = 'node.jpg';
  // // img.width = $(window).width();
  // // img.height = $(window).height()
  // // var str = '<div>'+img+'</div>'
  // // console.log(img);
  // // $('body').prepend($('body'),img);
  // $(window).on('resize',function(){
  //   $('.con > div').css({height:$(window).height()+'px'});
  // });
  // $('.con > div').css({height:$(window).height()+'px',position:'relative'});
  // $('.con .node').css('background','url("/images/node.jpg") no-repeat center/cover');
  // $('.con .html5').css('background','url("/images/html5.jpg") no-repeat center/cover');
  // $('.con .connect').css('background','url("/images/connect.jpg") no-repeat center/cover');
  // $('body').scroll(function(){
  //   console.log1(1);
  // });
  // $(window).scroll(function() {
  //   if ($(window).scrollTop() >= $('.con .node')[0].offsetTop) {
  //     $('.con .node .span').animate({opacity:1,left:'20%',bottom:'10%'},1000)
  //   };
  //   if ($(window).scrollTop() >= $('.con .html5')[0].offsetTop - 50) {
  //     $('.con .html5 .span').animate({opacity:1,top:'50%'},1000)
  //   };
  //   if ($(window).scrollTop() >= $('.con .connect')[0].offsetTop - 50) {
  //     $('.con .connect .span').animate({opacity:1,top:'20%',right:'10%'},1000)
  //   };
  // })
  // // console.log($('#nav').width(),$('footer').width(),$('.con').width(),$('body').width());
  // // $('.con .node').css
})
